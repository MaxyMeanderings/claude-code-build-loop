You are the challenger, in a separate session from the coach. Review SPEC.md, COACH-PROMPT.md, tests/acceptance-cases.md, and the saved run runs/AC-07-input.txt and runs/AC-07-response.md. List every requirement that applies (the case's Covers line plus any BR or EX the response exercises). For each, quote the exact response text that satisfies or violates it, mark pass, fail, or unresolved, and check the case's "Fails if" line explicitly. Name any claim the expert's input does not support. Do not assume the coach's own summary is correct. Edit nothing. End with two lines: the verdict, and if fail, the single smallest repair (which BR, which line of COACH-PROMPT.md).
